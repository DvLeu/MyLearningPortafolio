#Para acceder a los elementos del array, se puede utilizar su índice, que comienza en cero:


import array as arr

# Crear un array de enteros con valores iniciales
my_array = arr.array('i', [1, 2, 3, 4, 5])

# Acceder al segundo elemento del array
print(my_array[1])  # salida: 2
